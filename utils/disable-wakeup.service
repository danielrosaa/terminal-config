[Unit]
Description=Disable ACPI wakeup devices
After=multi-user.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'for dev in $(awk "/enabled/{print $1}" /proc/acpi/wakeup); do echo "$dev" > /proc/acpi/wakeup; done'
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
